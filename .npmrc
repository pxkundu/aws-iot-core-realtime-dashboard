; Skip optional dependencies that cause platform issues in CI
optional=false
; Disable package-lock shrinkwrap for better CI compatibility  
shrinkwrap=false
; Skip scripts that might fail in CI environments
ignore-scripts=false 